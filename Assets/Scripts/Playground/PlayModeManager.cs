using UnityEngine;

public class PlayModeManager : MonoBehaviour
{
    //In Play mode layers do not change, this results in the first layer of the grid being the default and player unable to
    //to choose any object that are in different layers

}
